import { Montserrat, Syne, Playfair } from 'next/font/google'
 
export const montserrat = Montserrat({
  subsets: ['latin'],
  display: 'swap',
})
 
export const syne = Syne({
  subsets: ['latin'],
  display: 'swap',
})

export const playfair = Playfair({
  subsets: ['latin'],
  display: 'swap',
})